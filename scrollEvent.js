window.addEventListener('scroll', () => {
  console.log(window.scrollX + 'px')
  console.log(window.scrollY + 'px')
})

//window.scrollX returns how many px are scrolled along x axis left or right

//this event is only firedup if we really have somthing to scroll to
